<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://www.mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.xwtec.ecu.web.search.dao.ISearchDao">

	<!-- 获取全部的方法 -->
	<select id="qryAllFunctions" resultType="java.util.Map">
		SELECT T.FUNCTION_ID AS id,
			   T.FUNCTION_NAME AS name,
		       T.FUNCTION_URL AS url
		  FROM T_FUNCTION_FLOOR_CONTENT T
		 ORDER BY T.SORT ASC
	</select>
	
	<!-- 获取全部的商品 -->
	<select id="qryAllGoods" resultType="java.util.Map">
		SELECT T.GOODS_ID AS id,
	       T.GOODS_NAME AS name,
	       T.GOODS_DESC AS descript
	    FROM T_HOME_GOODS T
	</select>
	
	<!-- 获取全部的卡 -->
	<select id="qryAllCards" resultType="java.util.Map">
		SELECT T.CARD_TYPE AS cardType, 
			T.CARD_NAME AS cardName
		FROM T_CARD_LIST_INFO T
		WHERE T.IS_USED='0'
	</select>
</mapper>